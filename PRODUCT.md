# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Chrome MV3 擴充功能：WXT + React 19 + TypeScript，建置鏈與前身相同（WXT、oxlint、Prettier、Vitest、Playwright 預覽腳本）。
本 repo（`biliEnhanceExt`）是 [biliFavOrg](https://github.com/1morr/biliFavOrg) 與
[biliFollowCleaner](https://github.com/1morr/biliFollowCleaner) 的合併：以 biliFavOrg 的基礎設施
（WBI 簽名、封面、字幕、AI client、影片頁 content script）為底，把 biliFollowCleaner 的關注清理整個搬進來。
兩個前身原地保留、不再發展。

## Users

一般 Bilibili 使用者（公開發佈，不預設熟人）。典型情境：收藏夾裡累積了幾百到幾千支影片、關注了幾百到幾千個帳號，
多年沒整理；偶爾（幾個月一次）坐在桌機前的 Chrome 裡、B 站已登入，願意花幾分鐘到幾十分鐘讓工具跑完，
然後一次看完、一次決定、一次執行。文案與防呆對象是陌生使用者：沒看過原始碼、不知道 B 站風控是什麼、也不會裝油猴腳本。

## Product Purpose

把兩件「先審核、再批次收拾」的事放進同一個擴充功能：

- **整理收藏**：把收藏夾裡的影片交給使用者自己選的 AI 端點分類，攤成審核表，確認後才搬移或複製；可撤銷。
  影片頁另有一顆「智慧收藏」按鈕，對眼前這一支影片做同一件事。
- **清理關注**：找出關注的帳號裡已經不再投稿的那些，把證據（最後一支影片、多久以前、分組）攤在審核表上，
  篩選、勾選、確認後才逐一取關；可撤銷。

成功＝收藏夾與關注清單都變整齊了，而且沒有任何一支影片、任何一個帳號是在使用者沒看過、沒確認的狀態下被動到。

## Positioning

同一個作者的兩個擴充功能做的是同一種事——讀 B 站資料 → 攤成證據齊全的審核表 → 使用者勾選確認 → 批次執行 → 可撤銷——
差別只在整理的對象是收藏夾還是關注清單。分成兩個擴充功能只是讓使用者裝兩次、學兩套設定、養兩份限速器。
合併後：一個工具列圖示、一份設定（AI 端點、限速、語言、快取）、一把 Web Lock、一個 IndexedDB、同一套視覺語言。

鄰居做不到的三件事原樣保留：**沒有你勾選並確認，什麼都不會動**；**狀態未確認的東西在結構上進不了任何批次**
（查不到活躍度的帳號、失效的影片）；**風控感知**（節流、退避、命中 -352／-412／-799 就整輪停下並保留進度）。

## Operating Context

- 執行環境：Chrome／Edge 114+，`chrome://extensions` 載入未封裝項目（是否上架商店未決定）。
- 整個流程在擴充功能自己的分頁跑（App 分頁），service worker 只做 DNR header 規則、開分頁、影片頁「智慧收藏」的單支往返。
- 資料來源全部是 B 站公開 API（`api.bilibili.com`，由 `declarativeNetRequest` 補 Referer/Origin）與使用者自己填的
  OpenAI 相容端點（只申請那一個 origin 的 optional host permission）。收藏夾端點見 `docs/design.md` 2，關注端點見 `docs/design.md` 3。
- 風控／錯誤碼共用一套：`-101` 未登入、`-111` csrf、`-352`／`-412`／HTTP 412／`-799` 風控與頻率限制、`-632` 數量限制、
  `22013` 帳號已註銷、`22014` 已關注、`22009` 關注上限、`40061` 用戶不存在。
- 兩種長任務（整理收藏、清理關注）**一次只跑一個**：共用同一把讀取節流器與 Web Lock；另一個任務進行中時，這邊的主按鈕變灰並寫出原因。
- 分頁切到背景時 Chrome 會節流計時器、幾分鐘後凍結；任務期間握 Web Lock 豁免凍結。
- 快取：影片詳情 30 天、封面 7 天、帳號活躍度 30 天（未知永遠重查），全部在同一個 IndexedDB。

## Capabilities and Constraints

已確認要做（兩個前身的功能全部保留）：

- 整理收藏：選來源夾與範圍 → 勾目標夾 → AI 分類（詳情／字幕／封面三個開關）→ 審核表（建議、理由、依據、低信心不預勾、
  「也留在原位」）→ 搬移／複製 → 撤銷；移除失效影片；收藏夾分頁維護描述（手寫、從 B 站匯入、AI 生成，都是草稿）；
  影片頁「智慧收藏」按鈕。
- 清理關注：準備畫面（關注數、預估請求與時間、只查缺的／全部重查、包含悄悄關注）→ 讀清單 → 逐帳號查最新投稿 →
  分面審核表（門檻、狀態、關注類型、分組）→ 取關（逐筆、二次確認）→ 撤銷（重新關注＋還原分組）→ 匯出 UID／CSV。
- 三態狀態模型：有影片／確認無影片／未知；未知帳號可見、可計數，**不可勾選、不進取關、不進匯出**。
- 設定一份：連線（AI 端點）、要給 AI 看什麼、要 AI 怎麼判斷、速度與資料（共用限速、三個快取、備份、語言）。
- 介面英文預設、繁體中文可切；文檔與註解繁體中文；程式碼識別符、log、commit 英文。

明確不做／未決定：

- 不做分組管理、不拉黑、不碰粉絲、不用動態判斷活躍度（與 biliFollowCleaner 一致）。
- 不提供刪除收藏夾（與 biliFavOrg 一致）。
- 兩種任務同時跑：**不做**（使用者決定：一次只跑一個，對風控最安全）。
- 頂列導覽：**四個平行分頁**（整理收藏／收藏夾／關注／設定），不做兩大區各自子分頁（使用者決定）。
- 是否上架 Chrome 商店：未決定。
- 特別關注在撤銷時能否透過 `tags/addUsers` 帶 `-10` 還原：未驗證（沿用前身的未解決事項）。

## Brand Commitments

- 名稱：**biliTidy**（使用者從三個候選裡選定；package 名 `bilitidy`、manifest 名、頂列品牌字一致）。MIT 授權。
- 視覺語言直接沿用兩個前身共用的「深色審核工作台」：同一組色票（B 站粉提亮為 `#ff5c8d`）、4px 圓角、1px 分隔線而非浮起卡片、
  狀態＝小色點＋文字、圖示一律畫成 SVG 不用 emoji、「頂列／內容／底部作業列」的滿版高度網格、
  底部作業列永遠是「你正要執行的批次＋執行它的按鈕，按鈕變灰時旁邊寫得出原因」。用過任一前身的人打開它要立刻認出是同一家的東西。
- 語氣：直白、不嚇人也不輕佻；講清楚「按下去會發生什麼」與「為什麼這一列不能選」。

## Evidence on Hand

- biliFavOrg 原始碼與文件（`docs/design.md`、`docs/how-it-works.md`、`docs/research/` 三份實測）：分類品質實測、
  「留在原地」的評估、WBI 測試向量、B 站收藏夾端點的查證。
- biliFollowCleaner 原始碼與文件（`PRODUCT.md`、`DESIGN.md`、`docs/design.md`、`docs/how-it-works.md`）：
  三態模型、關注端點的查證、分面審核表的結構決定。
- **沒有**使用者見證、沒有數據、沒有截圖可以杜撰；README 的截圖一律來自 `npm run ui-preview` 對假資料的實際渲染。

## Product Principles

1. **沒有你勾選並確認，什麼都不會動。** 預設不預選；主按鈕上永遠寫著數字。
2. **未確認不等於可以動手。** 查不到的帳號、失效的影片留在畫面上，但進不了批次。
3. **審核表就是產品。** 每一列都要看得到證據：AI 的理由與依據、最後一支影片與多久以前、分組與關注類型。
4. **尊重 B 站的節奏。** 節流、退避、風控即停、保留進度；兩種任務一次只跑一個，寧可讓使用者稍後再跑。
5. **一個工具、一份設定、一套語法。** 四個分頁的版面語法、控制項、狀態表達、底部作業列的意義都一樣。

## Accessibility & Inclusion

- 表格與作業列可完全用鍵盤操作；勾選框是原生 checkbox 換皮。
- 顏色永遠不是唯一訊號（狀態＝色點＋文字；風控／錯誤有文字說明）。
- 深色底上的文字與重點色對比 ≥ 4.5:1（沿用前身已驗算的色票）。
