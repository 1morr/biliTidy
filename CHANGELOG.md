# Changelog

本專案的所有重要改動都記在這裡。格式依 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版號依[語意化版本](https://semver.org/lang/zh-TW/)。條目寫「使用者看得到什麼變了」與非顯而易見的理由；
完整的設計取捨在 [docs/design.md](docs/design.md)，實測數據在 [docs/research/](docs/research/)。

合併前的歷史在兩個前身各自的 CHANGELOG：
[biliFavOrg](https://github.com/1morr/biliFavOrg/blob/master/CHANGELOG.md)、
[biliFollowCleaner](https://github.com/1morr/biliFollowCleaner/blob/master/CHANGELOG.md)。

## [Unreleased]

### Added

- **biliFavOrg 與 biliFollowCleaner 合併成一個擴充功能 biliTidy。** 兩者做的是同一種「先審核、再批次收拾」，
  分成兩個擴充功能只是讓人裝兩次、學兩套設定、養兩份限速器。現在：一個工具列圖示開一個分頁，
  頂列四個平行分頁——**整理**（收藏夾分類）、**收藏夾**（描述）、**關注**（清理不再投稿的帳號）、**設定**。
  兩個前身的功能全部保留：AI 分類、也留在原位、失效影片、影片頁「智慧收藏」、收藏夾描述的三條路；
  三態活躍度、分面審核表、逐筆取關、撤銷還原分組、UID／CSV 匯出。
- **一份設定。** 連線、要給 AI 看什麼、要 AI 怎麼判斷、速度與資料四段照舊；關注的門檻與「包含悄悄關注」
  仍在關注頁上改（改了就是改設定），設定頁不另放一份。設定 schema 多一段 `follows`；兩個前身的舊設定
  分別存在各自擴充功能的 storage 裡，不會被讀到，第一次打開就是預設值。
- **兩種長任務一次只跑一個。** 整理收藏與清理關注共用同一把讀取節流器與 Web Lock；一個在跑的時候，
  另一頁的主按鈕與寫入按鈕變灰、旁邊寫出「另一個任務還在跑——等它跑完，或到那一頁停止它」。
  互斥在 `ui/jobGuard.ts`，兩個 jobStore 開始任務前都要先取得執行權。
- **一個 IndexedDB（`bilitidy`），三個 store**：影片詳情、封面、帳號活躍度。設定頁的「快取」分成兩行各自清。
- 進度儀表（階段、`done/total`、已用與預估剩餘、速率、快取命中、最近幾筆請求、審核表骨架）收成一個共用元件
  `ui/components/ProgressPanel.tsx`，整理與關注都用它；讀數欄位由各頁自己決定。
- `npm run ui-preview` 一支腳本把四個頁面都走一遍（含互斥狀態與手機寬度、設定六章、時間軸播放頭的拖動），
  `npm run gen-icons` 由 SVG 原稿產生工具列圖示。

### Changed

- **介面重做：「彈幕播放器的時間軸」。** 整個 App 分頁換了一套視覺語言——影片黑的平塗底、1px 分隔線、不發光不投影；
  狀態色改用彈幕色板（黃＝安靜超過門檻／AI 草稿、綠＝還在投稿／完成、紅＝失敗、淡藍＝從未投稿／中性告知），
  粉色只給「現在／你選的／主要動作」；時間碼、計數、天數、mid 一律走內建的 Spline Sans Mono（`public/fonts`，OFL）。
  舊的「深色審核工作台」視覺被整組取代，理由與新規則見 `DESIGN.md`。
- **設定頁依「誰用得到」分三組六章。** 01 AI 端點、02 給 AI 看什麼、03 分類指示只有整理收藏（與收藏夾描述、影片頁）用得到；
  04 讀寫速度、05 快取與備份、06 語言兩個工具共用；左軌最後一列是通往關注頁的門——清理關注不需要 AI，
  它自己的門檻與「包含悄悄關注」仍在關注頁上改。每一章開頭寫明用在哪幾個功能、不用在哪個；
  每個設定寫明做什麼、預設是什麼；底部控制列的章節軌標出哪幾章改過沒存，主按鈕寫著改了幾項。
  舊的「速度與資料」拆成 04、05、06 三章，「語言」不再藏在速度那一段裡。
- **關注審核表多了一欄時間軸。** 每個帳號從最新一支投稿到今天畫成一條軌（超過門檻黃、沒超過綠；從未投稿與查不到是虛線），
  門檻是一條貫穿所有列的粉色播放頭，表頭上的圓鈕可以直接拖——拖了就是改門檻，與左軌的數字欄位是同一個設定。
  窄於 1200px 時這一欄先讓位給證據欄。
- **進度軌分成「已緩衝」與「已播」。** 任務進行中的進度條像播放器一樣：灰段是直接用快取答掉的、粉段是真的向 B 站打了請求的。
- **工具列圖示重畫成 SVG。** 原稿在 `public/icon/icon.svg`（影片黑瓦片＋白勾＋粉色進度軌與播放頭），
  `npm run gen-icons` 改用 Playwright 內建的 Chromium 轉成 16／32／48／128 PNG，不再用純 Node 逐像素畫；頂列品牌記號同一個符號。
  「測試視覺」送的是這張圖，確認題的描述跟著改。
- **名稱與識別。** Web Lock 改名 `bilitidy-job`、content script 的 DOM id 改為 `bilitidy-*`、備份檔的 `kind` 改為 `bilitidy-backup`
  （匯入不檢查 `kind`，biliFavOrg 匯出的備份照樣可以匯入）。
- **關注清理的預設讀取速率從 1 req/s 變成共用的 2 req/s**（biliFavOrg 實測站得住的預設）。限速三選一是
  保守 1／預設 2／快 3 req/s。查關注是每個帳號一個請求，關注幾千個的話設定頁的說明建議選「保守」；
  被限流時仍然是退避 60 → 120 → 240 秒後停下並保留進度。寫入間隔預設 800 ms（原 biliFollowCleaner 1000 ms）。
- 整理收藏進行中的儀表不再有第二顆「取消」按鈕——取消一律在底部作業列（與關注頁一致：作業列永遠是「這一批＋執行它的按鈕」）。
- 請求紀錄的端點簡名同時認得收藏夾端點（bvid／aid／media_id）與關注端點（mid／分頁／表單裡的 fid）。
- 介面文案的關注部分收在 `follows.*` 命名空間下；共用的頂列、進度儀表、錯誤與設定文案只有一份。
- 頂列在窄視窗（≤ 900px）時品牌只留記號、「重新檢查」只留圖示、帳號只留頭像，四個分頁放得下；整理頁與收藏夾分頁在窄視窗改成順流
  （左軌、中央、右欄上下排），不再是三欄並排把視窗撐寬。關注審核表在窄視窗維持堆疊列。
- **灰按鈕一律寫原因。** 整理頁審核作業列在「沒有任何一列要搬或複製」與「這一輪已經寫入」時、設定頁在「沒有未儲存的變更」時，
  主按鈕旁邊都會寫出為什麼按不下去（前身只在缺來源、缺端點、缺目標時寫）。
- 整理頁的失效影片列改用與關注頁「查不到」同一套鎖住的樣式：整列退到後面、狀態欄是鎖頭圖示＋「已失效」、滑過去寫著原因與清掉的方法。
- 「送了什麼」與「這次會做什麼」的檢視視窗不再投影、圓角回到 4px——這個世界只有焦點環一個 box-shadow。設定頁左軌的段落摘要固定一行。

### Fixed

- **關注審核表把幾乎每個帳號都寫成「沒有影片」。** 查活躍度用的 `series/recArchivesByKeywords` 不是投稿列表，
  是「推薦稿件」介面：帶 `ps=1` 時它對**每一個**帳號都回 `code 0` 加空清單，於是活躍帳號全被記成「從未投稿」，
  而「從未投稿」的不活躍天數是無限大——正好是會誤取關的方向。改走空間頁自己用的 `space/wbi/arc/search`
  （要 WBI 簽名），並且**只有 B 站回報投稿數為 0 才算沒有影片**；回應對不起來（說有投稿卻一支都沒回）一律當「查不到」。
  舊版留下的活躍度快取會在更新後自動作廢（紀錄 schema 跳號），不必手動清、也不用等 30 天 TTL。
- **分類失敗時畫面完全不說。** 沒填金鑰、端點回 401／500、AI 回傳解析不了——這些發生在「產生審核列之前」的失敗
  會把畫面丟回準備畫面，沒有橫幅、沒有一個字，錯誤訊息明明已經在手上卻沒有地方畫它（那塊橫幅只寫在審核畫面裡，
  而失敗時那個畫面正好進不去）。現在準備畫面會把原因寫出來，旁邊給一顆「打開設定」；
  寫入到一半失敗時也不再把審核表丟掉，主按鈕變灰時旁邊寫著「這一輪出錯停下了」。
- **不合法的 Base URL 會被默默換成 `https://api.openai.com/v1`，而你填的金鑰照樣留著。** 想跑區網模型的人
  存完設定會發現端點變成 OpenAI 的網址，下一次按「開始分類」金鑰就真的送出去了。現在填錯會即時標紅、
  主按鈕變灰、旁邊寫出規則（只接受 `https://`，本機模型可以用 `localhost`／`127.0.0.1`），而且**存不下去**——
  權限申請沒過也一樣不存。填壞的值退回「還沒填端點」而不是另一個端點；順帶讓「Base URL 留空」變成一個
  真的存在的狀態（以前空字串也會被換成預設端點，文檔描述的那條路走不到）。
- **API Key 標著必填但沒有任何地方檢查它，全新安裝一打開設定頁就顯示綠燈「已連線」。** 「已連線」現在只在
  「測試連線」通過之後才寫（`ai.connectionVerifiedAt`，改動端點／金鑰／模型就作廢），否則是「端點已填 · 未測試」；
  遠端端點沒填金鑰時，整理頁與收藏夾頁的主按鈕變灰並寫出原因，不必等讀完幾千支影片才收到一個 401。
  本機模型通常不收金鑰，所以 `localhost`／`127.0.0.1` 不擋，`API Key` 的「必填」標記也跟著端點走。
- **`npm run smoke` 是壞的**（版面重做之後 exit 1，而 CI 不跑它，所以沒人發現）：它先把語言設成繁體中文，
  再用約 30 個中文字串當選擇器，章節一改名整支就死了。現在跟著預設的英文語系跑，而且**能用結構就不用文字**——
  章節走畫面上的編號、分頁走順序、開關走順序，跟 `npm run ui-preview` 同一套做法。
- **一批做完之後，主畫面變成空表格。** 取關 27 個帳號、或把影片搬進收藏夾，做完的那幾列就不再符合目前的分面
  （關注頁預設是「安靜超過門檻」），於是中央區顯示「沒有帳號超過門檻」，剛做完最不可逆的那件事卻讀起來像
  「這裡沒事可做」——要確認做了什麼，得自己注意到左軌多了一個分面並點進去。現在批次一寫完，表格會**自己切到剛產生的那個分面**
  （已取關／已重新關注／已完成），上方一條綠色橫幅寫著這一輪做了幾筆、以及在清掉結果之前都還可以撤銷。
  `npm run ui-preview` 以前會自己去點那個分面再截圖，等於把畫面撥回正確狀態，所以截圖裡看不出這個問題；
  現在改成斷言分面已經自己亮了。
- **「收藏夾」分頁一顆都沒勾的時候，底部作業列一顆按鈕都沒有**，只剩一句話——第一次打開這一頁的人正好處在這個狀態，
  得從那句話裡自己推出「原來要先勾選」。現在三顆按鈕照樣在，全部是灰的，旁邊寫著原因，跟其他三頁一致。
- 兩份 README 說 `npm run gen-icons` 是「純 Node」，其實它跑 Playwright 的 Chromium；`PRODUCT.md` 把關注端點指到
  `docs/design.md` 3（那是「要給 AI 看什麼」，關注端點在 11）；README 的 CI 與授權 badge 還指著改名前的 repo。
  節號會漏是因為 `CLAUDE.md` 的同步指令只掃 `src docs CLAUDE.md README.md`，剛好掃不到 `PRODUCT.md`——改成掃全 repo。
- `npm run lint` 與 `npm run format:check` 不再掃 `docs/reviews/`（審查報告的附件是當時跑出來的樣子，不是專案原始碼）。
