# zhu1090093659/deepseek-pp · 1807 ★ · 最後 push 2026-08-13 · TypeScript（WXT + React MV3）· 約 138,083 行（670 個 .ts/.tsx 檔）

1. **README 結構**：`README.md`（簡中，預設）與 `README_EN.md`（英文）兩份平行維護。段落依序為：頂部 badge／語言切換連結 → 「產品定位」（一句話定位＋SEO 關鍵字段落）→「目錄」（錨點連結）→「功能速覽」→「適合場景」→「核心功能」（15 個子章節，每個功能一段＋部分含截圖說明）→ 從 1.14.0 到 0.2.0 的逐版「變更回顧」（約 50 個版本、每版一小節，內容等同 changelog 內嵌進 README）→「安裝」（Chrome Web Store／從原始碼建置／三瀏覽器對照表）→「友情連結」→「License」。**沒有獨立的疑難排解／FAQ 段落**；README 內文（`assets/readme-header.png`）有頂部橫幅圖但核心功能段落本身**沒有截圖或 demo GIF**。

2. **文檔目錄**：`docs/` 存在，非工具自動產生、全為手寫 Markdown。內容包括 `ci-cd.md`（CI/CD 說明）、`refactor-current-architecture.md`、`decisions/`（1 篇 ADR：`custom-css-theme-policy.md`）、`releases/`（每個版本一個 `<version>.md`，供 GitHub Release notes 用）、`compatibility/`（`README.md`＋4 份契約登記表：prompt-and-runtime、persistence-and-sync、platform-and-integrations、runtime-command-inventory，記錄「重構必須保留的外部可觀察行為」）、`verification/`（MCP mock／operator 筆記）、`analysis/`（2 篇專題分析）、`archives/`（多個已完成的重構專案封存，含 progress/analysis/plan 三段式紀錄）、`chrome-web-store/`（上架素材，見第 9／14 項）、`superpowers/specs/`（1 份設計規格）。

3. **CHANGELOG 與版本**：沒有根目錄 `CHANGELOG.md`；版本紀錄拆成兩份——`docs/releases/<version>.md`（給 GitHub Release 用的簡短說明）與 README 內逐版「變更回顧」章節（面向使用者的詳細說明，簡中／英文各一份）。版本號依 SemVer，用 `v*.*.*` git tag 觸發 release workflow；近期節奏約每 1–3 天一版（v1.14.0 2026-08-08、v1.13.1 2026-08-06、v1.13.0 2026-08-05、v1.12.2 2026-08-04、v1.12.1 2026-08-04、v1.12.0 2026-08-03、v1.11.9 2026-08-02、v1.11.8 2026-07-27）。GitHub Release 由 `.github/workflows/release.yml` 全自動建立並附 4 個資產。

4. **貢獻流程**：有 `CONTRIBUTING.md`（5 條 PR 規則：基於最新 main、聚焦單一變更、本機驗證、使用者可見變更需附證據、不得隱藏失敗）；有 `.github/pull_request_template.md`（含 Summary／PR Type／Latest Codebase Confirmation／**AI Coding Disclosure**〔要求揭露是否用 AI 及用哪個模型／哪個 coding agent〕／Local Validation／Local Feature Evidence）；有 `.github/ISSUE_TEMPLATE/`（`bug_report.yml`、`standard_issue.yml`、`config.yml`），且有專用 workflow（`issue-template-enforcer.yml`）自動關閉未照模板填寫的 issue、`pr-contribution-rules.yml` 自動檢查 PR body 是否缺必填欄位並留言。**沒有** `CODEOWNERS`、`CODE_OF_CONDUCT.md`。

5. **目錄結構**：頂層（略過 `node_modules`／`.git`／`videos` 推廣影片素材）為 `core/`、`entrypoints/`、`packages/`（npm workspaces，內含 `shell-host/`）、`public/`、`scripts/`、`tests/`、`docs/`、`assets/`、`.github/`。原始碼切分以 **feature/domain 為主**：`core/` 下平鋪約 35 個功能目錄（`memory`、`skill`、`mcp`、`automation`、`inline-agent`、`sync`、`persistence`、`browser-control`、`tool-loop`、`sandbox`、`deepseek`…），沒有 `core/utils` 這類雜物桶；`entrypoints/` 依 MV3 執行環境分（`background`、`content`、`sidepanel`、`sandbox-offscreen`、`sandbox-runner`），`sidepanel` 底下再依 `components`／`controllers`／`pages` 分層。最深路徑在 `core/skill/officecli-official/skills/morph-ppt/reference/styles/<style-name>/`（第 8 層，屬於內建 Skill 的參考資料，非程式邏輯）；一般程式碼路徑多落在第 3–5 層。

6. **模組邊界**：共用/契約型程式碼集中在 `docs/compatibility/`（非程式碼，而是「外部可觀察行為契約」登記表，供重構時對照）與 `AGENTS.md`（單一專案指令真源，取代 CLAUDE.md，明文寫「不建立第二份記憶檔案」）。`AGENTS.md` 用大量條列規則明確規定依賴方向與邊界，例如：「Contract modules must not import concrete browser, DOM, provider, or entrypoint implementations」「Background entrypoints are composition/lifecycle roots; domain behavior belongs in typed handlers and services」「Every production tool execution must pass through the runtime authorization path」，以及針對 sync／automation／content capability 各自的單一權威（single source of truth）規則。這些規則寫在文檔（`AGENTS.md`）裡，未見對應的 lint 規則（如 import 邊界檢查工具）強制執行。

7. **靜態檢查**：`tsc --noEmit`（`npm run compile`，`tsconfig.json` extends `.wxt/tsconfig.json`）是唯一的靜態型別檢查。**沒有找到任何 lint 或 format 工具設定**——repo 內無 `.eslintrc*`、`.prettierrc*`、`.oxlintrc*`、`biome.json`、`.editorconfig`，`package.json` 的 `scripts` 也沒有 `lint` 或 `format` 指令。強制點：CI 的 `ci:quality`（`npm run compile` 是其中一步）；未見 pre-commit hook（無 `.husky/`、無 `.pre-commit-config.yaml`）。

8. **測試**：框架為 Vitest（`vitest.config.ts`：`environment: 'jsdom'`，`include: ['tests/**/*.test.ts']`）。`tests/` 目錄下有 **218 個 `*.test.ts` 檔**（皆為單一 flat 目錄，另有 `tests/fixtures/`、`tests/helpers/` 子目錄放測試資料與輔助函式），對應 `core/`＋`entrypoints/`＋`packages/` 共 427 個原始 `.ts/.tsx` 檔（測試檔數約為原始檔數的一半）。測試性質以**單元／整合測試為主**（例如 `automation-execution.test.ts`、`background-tool-authorization-integration.test.ts`、`artifact-indexeddb-compatibility.test.ts`），並有專門的 **prompt 契約凍結測試**（`tests/prompt-output-contract.test.ts`，由 `scripts/prompt-freeze.mjs` 執行、`npm run prompt:freeze:update` 才能改動）與 **inline-agent 事件協議黃金測試**（`tests/inline-agent-event-protocol-golden.test.ts`）；未見瀏覽器端到端（Playwright/Puppeteer）測試，`docs/compatibility/README.md` 記載重構基線時為「63 個測試檔／359 個測試」（現已成長到 218 檔）。CI（`ci:quality`）會跑 `npm test`。

9. **CI/CD**：`.github/workflows/` 有 5 個 workflow。
   - `ci.yml`：PR／push main／手動觸發，單一 job「Quality gates」跑 `npm run ci:quality`（見下方逐步展開）。
   - `release.yml`：push `v*.*.*` tag 或手動輸入 tag 觸發；驗證 tag 對應的 commit、跑 `ci:quality`、把 `packages/shell-host` 發布到 npm（有版本已存在則跳過、輪詢確認發布成功）、驗證 `dist/` 下剛好 4 個 zip、用 `gh release create/upload` 建立/更新 GitHub Release 並帶 `docs/releases/<version>.md` 當 release notes。
   - `chrome-web-store.yml`：手動觸發（`dry_run`／`submit_review` 開關），跑 `ci:quality` 後呼叫 `npx wxt submit`（帶 `CHROME_EXTENSION_ID`／`CHROME_CLIENT_ID`／`CHROME_CLIENT_SECRET`／`CHROME_REFRESH_TOKEN` 四個 secrets）。
   - `pr-contribution-rules.yml`：`pull_request_target`，用 `actions/github-script` 解析 PR body 各段落，缺 PR Type／Latest Codebase Confirmation／Local Validation 就留言＋`core.setFailed`；非 repo owner 的使用者可見功能 PR 還要求附截圖/錄影證據。
   - `issue-template-enforcer.yml`：`issues: opened/reopened`，解析 issue body 缺哪個必填段落（Bug report 額外要求截圖）就自動留言並把 issue 關閉（`state_reason: not_planned`）。
   `npm run ci:quality`（root gate，`package.json` script）依序串接：`verify:workflows`（`actionlint`）→ `audit:prod`（`npm audit --audit-level=high --omit=dev`）→ `prompt:freeze` → `compile`（tsc）→ `test`（vitest）→ `verify:persistence-burst` → `verify:i18n`（`i18n-locale-check.mjs` + `i18n-coverage-audit.mjs`）→ `verify:automation` → `smoke:mcp` → `verify:mcp:mock` → `smoke:shell` → `smoke:pow` → `build:all`（chrome/edge/firefox 三個瀏覽器都建置）→ `verify:bundled-skills` → `verify:sidepanel-chunks` → `verify:extension-utf8` → `verify:manifest-policy` → `zip:all` → `verify:release-assets` → `smoke:pyodide`。Release 是**全自動**（tag push 即觸發打包＋發布＋建 GitHub Release），Chrome Web Store 上傳則保留手動觸發（`workflow_dispatch`）當閘門。

10. **依賴管理**：`package-lock.json` 有入庫（343KB）。版本釘選以 caret（`^`）range 為主（如 `react": "^19.2.4"`、`wxt": "^0.20.26"`），但 `AGENTS.md` 明文要求 `@earendil-works/pi-agent-core` 與 `@earendil-works/pi-ai` **必須精確鎖版**（無 `^`/`~`），package.json 中兩者確實無 range 前綴；另用 `overrides` 欄位釘住 `tmp`、`uuid`、`@earendil-works/pi-ai` 三個傳遞依賴版本。npm workspaces 只有 `packages/*`（實際僅 `packages/shell-host` 一個子套件）。**沒有** Dependabot（`.github/dependabot.yml` 不存在）或 Renovate 設定；CI 有 `npm audit --audit-level=high --omit=dev` 作為生產依賴稽核。

11. **設定與環境**：**沒有 `.env.example`**；`.gitignore` 中列出 `.env`、`.env.submit`、`.env.chrome-web-store`（供 `wxt submit`／Chrome Web Store 憑證使用，但倉庫內無範本檔說明格式）。Secrets 集中在 GitHub Actions repo secrets（`CHROME_EXTENSION_ID`／`CHROME_CLIENT_ID`／`CHROME_CLIENT_SECRET`／`CHROME_REFRESH_TOKEN`／`NPM_TOKEN`），皆以 `secrets.*` 形式注入 workflow，未見任何明文憑證。主設定檔為 `wxt.config.ts`（10KB，WXT/MV3 manifest 設定）、`tsconfig.json`、`vitest.config.ts`；使用者可設定項（AI 端點、MCP、記憶等）存於瀏覽器 IndexedDB／storage，非檔案化設定。

12. **commit 慣例**：抽樣近 30 條，明顯採用 **Conventional Commits 前綴**（`fix(scope):`、`feat(scope):`、`chore(scope):`、`docs:`、`test(scope):`）但 commit 內文（subject 與 body）多為**簡體中文**、subject 常附 `（issue #NNN）`；也有不遵循前綴格式的自由文字 commit（例如 `Release DeepSeek++ 1.14.0`、`Refine extension runtime workflows and compatibility contracts`、`Merge pull request #549 from ...`）。**沒有找到 commitlint 設定**（無 `.commitlintrc*`、`commitlint.config.*`，`package.json` 亦無相關 devDependency）。PR 大小從抽樣的 commit/PR 標題看差異大，從單一小修復（`fix(interceptor): suppress raw tool tags after augmentation failure`）到跨檔案的整批重構（`Refine extension runtime workflows and compatibility contracts`）都有，多筆 merge commit 顯示採 PR 合併（`Merge pull request #NNN from zhu1090093659/codex/...`），分支命名可見 `codex/issue-<n>` 這類 AI coding agent 產生的分支。

13. **授權與元資料**：`LICENSE` 為 Apache License 2.0 全文；`package.json` 頂層無 `license` 欄位重複標註於 LICENSE 外（實際上 `package.json` 內有 `"license": "Apache-2.0"`）；**沒有 `SECURITY.md`**（`AGENTS.md` 內有一段「Security Baseline」文字規則，但無獨立安全回報政策檔）；**沒有 `NOTICE`** 檔案。`package.json` 元資料：有 `name`／`description`／`private: true`／`license`／`version`／`type: module`／`workspaces`，**沒有** `repository`、`author`、`keywords`、`homepage`、`bugs` 欄位（這些資訊改放在 README 的 badge 連結與 `docs/chrome-web-store/listing.md` 裡）。

14. **開發者體驗**：無一鍵啟動腳本（`npm run dev` 走 WXT 標準 dev server），無 devcontainer，無 Makefile/justfile。`scripts/` 目錄下 22 個 `.mjs`／`.json` 腳本，命名一致採 `<動作>-<對象>.mjs`（`verify-*`、`smoke-*`、`*-check.mjs`）風格，由 `package.json` 的 `scripts` 欄位統一掛號呼叫，而非直接手動執行。**上架相關腳本／流程逐條列出**（對應 `wxt submit`／`zip`／`verify:*`）：
    - `build` / `build:chrome` / `build:edge` / `build:firefox` / `build:all`：呼叫 `wxt build`，分別產出 Chrome／Edge／Firefox（MV3）三份建置產物，`build:all` 三個一次做完。
    - `zip` / `zip:chrome` / `zip:edge` / `zip:firefox`：呼叫 `wxt zip` 打包對應瀏覽器的上架 zip。
    - `zip:sources`：執行 `scripts/package-sources.mjs`，用 `git` 命令把原始碼（非建置產物）打包成 `dist/deepseek-plus-plus-<version>-sources.zip`（Chrome Web Store 稽核常要求的原始碼佐證包）。
    - `zip:all`：依序做 chrome／edge／firefox／sources 四個 zip。
    - `submit:chrome:dry`：`wxt submit --dry-run`，只檢查 Chrome Web Store API 憑證是否有效，不上傳。
    - `submit:chrome:upload`：`wxt submit --chrome-skip-submit-review`，上傳新版但不送審（先讓套件進 Draft）。
    - `submit:chrome`：`wxt submit --chrome-publish-target default`，上傳並直接送出正式發布審核。
    - `verify:manifest-policy`（`scripts/manifest-policy-check.mjs`）：讀取 `dist/{chrome,edge,firefox}-mv3/manifest.json`，核對每個瀏覽器 manifest 宣告的權限清單（`storage`／`alarms`／`nativeMessaging`／`contextMenus`／`offscreen`／`debugger`／`tabs`／`identity`／`sidePanel`）是否與政策一致——即**manifest 政策檢查腳本**。
    - `verify:extension-utf8`（`scripts/verify-extension-utf8.mjs`）：掃描三個瀏覽器建置產物中的 `.css/.html/.js/.json`，用嚴格 UTF-8 解碼器確認沒有編碼錯誤（避免上架後多語系亂碼）。
    - `verify:sidepanel-chunks`（`scripts/sidepanel-chunk-budget.mjs`）：檢查各瀏覽器建置後 sidepanel 的 JS chunk gzip 後大小是否超出手動維護的基準值（budget），程式內以註解記錄每次基準重新校正的原因（例如新功能加入後重新量測）。
    - `verify:release-assets`（`scripts/release-assets-check.mjs`）：**release asset 檢查腳本**，核對 `dist/` 下依版本號命名的 4 個 zip 是否齊全、比對打包內容是否符合 `pyodide-package-policy.json`／`bundled-skill-package-policy.json` 兩份政策（例如捆綁的 Pyodide／Skill 檔案是否被意外變動）。
    - `verify:bundled-skills`（`scripts/bundled-skill-package-check.mjs`）：對照 `bundled-skill-package-policy.json`，檢查內建 Skill（`core/skill/officecli-official`、`core/skill/spec-driven-develop-official`）打包後的 gzip 大小分組是否符合預期。
    - `verify:i18n`（`scripts/i18n-locale-check.mjs` + `scripts/i18n-coverage-audit.mjs`）：**i18n 覆蓋率檢查腳本**——前者核對 `public/_locales/{en,zh_CN}/messages.json` 的必要鍵（如 `extension_name`）都存在且一致；後者掃描 `core`／`entrypoints`／`scripts`／`public/_locales/en` 等目錄找殘留的簡體中文字元（`\p{Script=Han}`），只允許在明確列出的 allowlist 路徑（附原因註解）出現，避免英文語系或程式邏輯裡混入未抽出的中文字串。
    - `verify:workflows`（`actionlint`）：檢查 `.github/workflows/*.yml` 語法與常見錯誤。
    - `verify:automation`（`scripts/automation-contract-smoke.mjs`）：檢查 automation／inline-agent 相關核心檔案（`core/automation/*.ts`、`core/inline-agent/*.ts`、`core/deepseek/active-client.ts` 等）是否存在，作為契約完整性的輕量煙霧測試。
    - `verify:persistence-burst`：跑 `tests/persistence-burst-budget.test.ts`，屬效能／負載型測試而非單純 unit test。
    - `smoke:mcp` / `verify:mcp:mock` / `smoke:shell` / `smoke:pow` / `smoke:web` / `smoke:pyodide`：分別對 MCP、Shell Native Host、DeepSeek PoW 挑戰、線上網頁搜尋、Pyodide 離線環境做端到端煙霧測試（`smoke:web` 因依賴外部 Bing 搜尋被刻意排除在必要 CI gate 外，只在 release 前手動確認）。
    - `prompt:freeze` / `prompt:freeze:update`：跑（或更新）prompt 位元組黃金快照測試，防止 prompt／工具 XML 格式被意外改動。
    這些檢查絕大多數只在 CI 的 `ci:quality` 一次性串起執行，沒有獨立區分「PR 快速檢查」與「release 前完整檢查」兩層 CI gate。
