# wenyuanw/quick-prompt · 849 ★ · 最後 push 2026-08-25 · TypeScript / React 19 + WXT · 約 28,700 行

（行數統計：`.ts`／`.tsx`／`.js`／`.jsx`，排除 `node_modules`／`.git`／`.output`／`.wxt`，共 28,652 行，含測試檔）

1. **README 結構**：`README.md`（簡體中文）與 `README_en.md`（英文）並存，內容一一對應。段落依序為：頂部 banner 圖＋多語言連結（連到 openaitx.github.io 的自動翻譯頁，涵蓋 20 種語言）、一句話定位（h1 + 一段描述）、「✨ 功能特性」（條列式功能清單）、「🚀 使用方法」（編號步驟）、「📸 界面预览」（4 組功能各配 GitHub CDN 截圖，無 GIF/demo 影片）、「⚙️ 自定义配置」、「📦 安装指南」（分「從應用商店安裝」「從 GitHub Releases 獲取」「從源碼構建」「安裝已構建的擴充」四小節）、「📄 许可证」（MIT 一行）、「🤝 贡献指南」（5 步 fork/branch/commit/push/PR 流程）、「👏 贡献者名单」（contrib.rocks 圖）、「Star History」圖表。**沒有「需求」「疑難排解」獨立段落**。

2. **文檔目錄**：有 `docs/`，內容三份：`docs/notion-sync-guide.md`（Notion 同步設定教學，手寫步驟指南，非 API 文檔）；`docs/superpowers/plans/2026-04-26-prompt-attachments-webdav.md` 與 `docs/superpowers/specs/2026-04-26-prompt-attachments-webdav-design.md`——這兩份是「superpowers」這套 agentic 開發流程產生的單一功能（附件＋WebDAV）的 spec 與 plan 文件，plan 檔開頭寫明「REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development...」，只涵蓋這一個歷史功能，非全專案架構文檔。沒有 ADR 目錄、沒有 API 文檔產生工具（無 TypeDoc/Docusaurus 之類設定）。

3. **CHANGELOG 與版本**：有 `CHANGELOG.md`（235 行），格式接近 Keep a Changelog 但分類用中文（`### 新增`／`### 优化`／`### 修复`），版本號用 `## [x.y.z] - YYYY-MM-DD`，部分條目附 PR 連結（如 `[PR#60](...)`）。`package.json` 目前 `version: "2.0.4"`。Git tag 從 `0.0.1` 一路到 `2.0.4`，每個 tag 都對應一個 GitHub Release，Release 內文直接照抄該版本在 CHANGELOG 的段落（人工維護、非自動產生，因為 repo 內沒有任何 CI/release workflow）。版本節奏：早期（0.0.x）近乎每週甚至更密，2.0.x 系列大約每 3–7 週一版（2.0.3 → 2.0.4 相距約 6 週）。

4. **貢獻流程**：沒有 `CONTRIBUTING.md`、沒有 `CODEOWNERS`、沒有 `CODE_OF_CONDUCT.md`、沒有 `.github/` 目錄（因此也沒有 issue template、PR template）。貢獻步驟只寫在 README 的「🤝 贡献指南」5 步（fork → 建分支 → commit → push → PR），無額外檢查清單。倒是有 `AGENTS.md`（給 AI coding agent 看的專案指引）與 `CLAUDE.md`（Claude Code 專用指引），兩者都寫了目錄結構、指令、命名慣例。

5. **目錄結構**：頂層（扣掉 `.git`/`node_modules`/`.output`）為 `assets/`、`components/`、`docs/`、`entrypoints/`、`lib/`、`public/`、`tests/`、`types/`、`utils/`，另有 `AGENTS.md`、`CLAUDE.md`、`CHANGELOG.md`、`LICENSE`、`README.md`／`README_en.md`、`package.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml`、`tailwind.config.js`、`tsconfig.json`、`vitest.config.ts`、`vitest.setup.ts`、`wxt.config.ts`、`.env.example`。原始碼切分是「先 by-layer（`entrypoints` 進入點 / `components` 共用元件 / `utils` 共用邏輯 / `lib` 通用小工具），entrypoints 內部再 by-feature」：`entrypoints/{content,options,popup,sidepanel}` 各自有自己的 `components/`（`content` 另有 `utils/`）。`components/` 下再分 `common/`（跨頁共用狀態元件，如 `EmptyState.tsx`/`ErrorState.tsx`/`PageHeader.tsx`）、`layout/`（`AppShell.tsx`）、`ui/`（shadcn/ui 風格的原子元件，如 `button.tsx`/`dialog.tsx`/`select.tsx` 等 18 個檔）。`utils/` 下再分 `attachments/`、`auth/`、`browser/`、`sync/` 四個子目錄，加上一層根層工具檔（`categoryUtils.ts`/`promptStore.ts`/`variableParser.ts` 等）。最深路徑是 `entrypoints/content/components/` 與 `docs/superpowers/plans/`，皆為 3 層。

6. **模組邊界**：共用程式碼集中在 `utils/`（業務邏輯，依子資料夾分同步管道/附件/認證/背景腳本封裝）、`components/`（UI）、`types/`（`global.d.ts`）、`lib/utils.ts`（`cn()` 之類 shadcn 慣用工具）。`CLAUDE.md` 用一節「Architecture」文字描述各層職責（entrypoints/utils/components），`AGENTS.md` 也重複類似清單並註明路徑別名 `@/` 指專案根、`#imports` 指 WXT 產生的自動 import。**沒有用 lint 規則（如 `eslint-plugin-boundaries`／`import/no-restricted-paths`）強制依賴方向**——因為專案裡根本沒有 ESLint 設定；邊界純粹是文檔約定與目錄命名習慣。

7. **靜態檢查**：`package.json` 的 `scripts` 只有 `dev`/`dev:firefox`/`build`/`build:firefox`/`zip`/`zip:firefox`/`compile`/`test`/`test:run`/`test:coverage`/`postinstall`——**沒有 `lint` 或 `format` 腳本**。全 repo（含子目錄，排除 `node_modules`）搜尋 `.eslintrc*`／`eslint.config*`／`.prettierrc*`／`.oxlintrc*`／`.editorconfig`，**一個都沒有**。`AGENTS.md` 第 16 行明寫「No ESLint/Prettier in repo; keep diffs small and consistent with nearby code.」。Type check 用 TypeScript 5.8.3：根目錄 `tsconfig.json`（4 行）只有 `"extends": "./.wxt/tsconfig.json"` 加 `allowImportingTsExtensions: true`、`jsx: "react-jsx"`；真正的編譯選項在 `wxt prepare`（`postinstall` 腳本）產生的 `.wxt/tsconfig.json`（此檔不進版控），其中第 8 行 `"strict": true`（另外 `forceConsistentCasingInFileNames`／`skipLibCheck` 皆為 true）。沒有 `.github/` 目錄，因此**沒有任何 CI 會跑 `pnpm compile`／`pnpm test`／格式檢查**；`.git/hooks` 下只有 Git 內建的 `.sample` 檔，沒有 husky 或其他 pre-commit hook——靜態檢查完全靠開發者手動執行，不在任何地方被強制。

8. **測試**：框架為 Vitest 4.0.16（`vitest.config.ts`：`environment: 'jsdom'`、`setupFiles: ['./vitest.setup.ts']`、`include: ['tests/**/*.test.{ts,tsx}']`、coverage provider 為 v8 只統計 `utils/**/*.ts`），另用 `@testing-library/react` 與 `@testing-library/jest-dom` 做元件測試，`jsdom` 27.3.0 模擬 DOM。`tests/` 目錄下共 **37 個 `*.test.ts`/`*.test.tsx` 檔**（另有 1 個非測試的 mock 輔助檔 `tests/mocks/imports.ts`，用來假 WXT 的 `#imports` 自動匯入），分佈在 `tests/content/`（6 個，含 `PromptSelector.test.tsx`／`quickInject.test.ts`）、`tests/options/`（12 個，含 `WebDavIntegration.test.tsx`／`PromptFormAttachments.test.tsx`）、`tests/utils/`（19 個，涵蓋各同步管道如 `feishuSync`/`gistSync`/`webdavSync`/`zipArchive`）。對照的原始碼檔（`.ts`/`.tsx`，排除 `tests/`、`node_modules`、`.output`、`.wxt`）共 **110 個**，測試檔／原始檔比約 37:110（約三分之一覆蓋率，且集中在 `utils/` 與部分 `options`/`content` 元件，`popup`/`sidepanel`/`entrypoints/background.ts` 等大面積無對應測試）。全部是單元／元件層級測試，**沒有 e2e 測試**（無 Playwright/Cypress 設定）。`package.json` 有 `test`/`test:run`/`test:coverage` 三個腳本，但因為沒有 `.github/`，**CI 完全不跑測試**，執行與否全靠開發者自律。

9. **CI/CD**：`.github/` 目錄不存在，**沒有任何 GitHub Actions workflow**，因此建置、測試、lint、發版皆無自動化。Release 流程是人工打 tag＋在 GitHub 網頁手動建立 Release、貼上 CHANGELOG 對應段落文字（見第 3 項）。

10. **依賴管理**：用 pnpm，`pnpm-lock.yaml` 已入庫，另有 `pnpm-workspace.yaml`（只設定 `onlyBuiltDependencies: [sharp]` 與 `allowBuilds` 提示，非 monorepo 多套件配置）。`package.json` 的 `dependencies`／`devDependencies` 全部用 `^` caret range 釘選（例如 `"react": "^19.1.0"`、`"wxt": "^0.20.7"`），沒有精確釘死版本。**沒有 dependabot（`.github/dependabot.yml` 不存在）也沒有 renovate 設定**。`package.json` 缺 `author`／`repository`／`keywords`／`license`／`homepage` 等欄位（只有 `name`／`description`／`private`／`version`／`type`／`scripts`／依賴清單）。

11. **設定與環境**：專案設定分散在多個檔案——`wxt.config.ts`（manifest 內容、OAuth client id、快捷鍵綁定）、`tailwind.config.js`、`tsconfig.json`、`vitest.config.ts`。`.env.example` 內容只有 3 個變數：`WXT_CHROME_APP_CLIENT_ID_PREFIX`、`WXT_WEB_APP_CLIENT_ID_PREFIX`、`WXT_FIREFOX_EXTENSION_ID`，用途是 Google OAuth（Chrome/Web client id 前綴）與 Firefox 擴充 ID；`wxt.config.ts` 用 `import.meta.env.WXT_*` 讀取，讀不到時退回寫死在檔案裡的預設 client id（第 18–20 行）。Secrets 處理方式是 `AGENTS.md` 明文提醒「Never commit secrets. Copy `.env.example` → `.env`」，沒有更進一步的 secret-scanning 或 vault 機制。使用者資料存放於 `browser.storage.local`（`utils/constants.ts` 定義的 storage key），無後端伺服器。

12. **commit 慣例**：最近 30 條 log 混合三種風格——(a) 標準 Conventional Commits 前綴（`feat:`／`fix:`／`docs:`／`chore:`／`refactor:`），(b) 同樣前綴但加 gitmoji（如 `✨ feat: ...`、`🐛 fix: ...`、`📝 docs: ...`），(c) 非標準的 `release: bump version to 2.0.3 and add new side panel feature` 這類發版 commit（用完整句子描述而非 Conventional Commits 的 `chore(release):` 格式）。另外常見 `Merge pull request #N from <user>/<branch>` 的合併 commit（非 squash merge）。**沒有 commitlint 設定**（找不到 `commitlint.config.*`／`.czrc`）。PR 大小：抽樣 8 個近期已合併 PR（#57/#58/#60/#62/#63/#66/#67/#68），改動行數從 9 行到 17,940 行（#57 為附件+WebDAV 大功能，含鎖檔異動，屬離群值），扣除該離群值後其餘 7 個 PR 平均約新增 826 行、變動 13.7 個檔案，PR 規模差異相當大，無固定上限。

13. **授權與元資料**：有 `LICENSE`（MIT，版權人 `wenyuan`，2025 年）。**沒有 `SECURITY.md`**。`package.json` 元資料不完整（見第 10 項，缺 `author`/`repository`/`license`/`keywords` 欄位），license 資訊只存在於 `LICENSE` 檔與 GitHub repo 設定（`gh api` 回報 `license.spdx_id = MIT`），未同步進 `package.json`。

14. **開發者體驗**：沒有一鍵啟動腳本（`Makefile`/`justfile` 不存在），沒有 devcontainer（`.devcontainer/` 不存在）。開發指令都走 `pnpm`（`pnpm dev`／`pnpm dev:firefox`／`pnpm build`／`pnpm build:firefox`／`pnpm zip`／`pnpm zip:firefox`／`pnpm compile`），命名採動詞直述、依平台（Chrome 為預設、Firefox 加 `:firefox` 後綴）與動作（`dev`/`build`/`zip`）二維排列，無自訂 shell script 目錄（無 `scripts/`）。`postinstall` 自動跑 `wxt prepare` 產生型別與 `.wxt/`，`pnpm install` 後裝機即可 `pnpm dev`，屬於「安裝即可跑」但沒有額外的環境檢查或引導腳本。
